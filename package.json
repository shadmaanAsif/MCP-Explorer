{
  "name": "mcp-demo",
  "version": "0.1.0",
  "description": "A learning project: building an MCP server one stage at a time.",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "npm run build && npm run start",
    "client": "node build/client.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^22.10.5"
  }
}
